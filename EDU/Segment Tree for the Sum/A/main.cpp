#include <bits/stdc++.h>

using namespace std;

vector<int> tree;

void buildSegment(vector<int> a, int l, int r, int pos) {

}

int main() {
    int n,m;
    cin >> n >> m;

    vector<int> v(n);

    for(int i = 0; i < n;i++) {
        cin >> v[i];
    }



    return 0;
}
